cobrapy demo and binding
========================

The demo file uses the cobrapy tutorial to illustrate how a cobra model can be balanced using the service.

A reaction is deliberately disrupted and then corrected using the service.

The demo illustrates how to 'cobra_balance.py' as a module.